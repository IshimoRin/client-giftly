import 'package:flutter/material.dart';

class AiHelperPage extends StatelessWidget {
  const AiHelperPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ИИ Помощник'),
      ),
      body: const Center(
        child: Text('Страница ИИ помощника'),
      ),
    );
  }
} 